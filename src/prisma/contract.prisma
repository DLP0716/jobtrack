// use prisma-next
model Application {
  id        String   @id @default(uuid())
  company   String
  position  String
  status    String
  appliedAt temporal.timestamptz(3)
  createdAt temporal.createdAt()
  updatedAt temporal.updatedAt()
}
